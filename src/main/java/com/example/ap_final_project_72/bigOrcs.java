package com.example.ap_final_project_72;

public class bigOrcs extends Orcs{

    Coordinates coordinates;

    public bigOrcs(double x, double y) {
        coordinates = new Coordinates(x, y);
    }
}
